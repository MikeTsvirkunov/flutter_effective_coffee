import 'package:flutter/material.dart';

class ErrorProcessedWidget extends StatelessWidget {
  const ErrorProcessedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('cannot get data');
  }
}
